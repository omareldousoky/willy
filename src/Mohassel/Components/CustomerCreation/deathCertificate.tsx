import React, { Component } from 'react'
import Swal from 'sweetalert2';
import Button from 'react-bootstrap/Button';

export default class DeathCertificate extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
